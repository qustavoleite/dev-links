o que aprendi
- mobile first
- conceitos da propriedades display
- conceito de webkit
-aplicra icones utilizando o ionicons

/* if (html.classList.contains("light")) {
       html.classList.remove("light");
     } else {
       html.classList.add("light");
     }
   */